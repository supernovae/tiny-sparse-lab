# Loss

Mean negative log-probability of the next target token, in nats. Lower values are comparable only when tokenizer, data, and budget match. Produced by `training.trainer` and `evaluation.language_model`.
